{
  "name": "moonrat",
  "version": "0.1.0",
  "private": true,
  "description": "Moonrat — a story-driven meme token & mining game Telegram Mini App on TON. MINE • HOLD • EXPLORE.",
  "workspaces": [
    "server",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=@moonrat/miniapp",
    "dev:server": "npm run dev --workspace=server",
    "dev:miniapp": "npm run dev --workspace=@moonrat/miniapp",
    "dev:admin": "npm run dev --workspace=@moonrat/admin",
    "build": "npm run build --workspaces --if-present",
    "db:setup": "npm run db:setup --workspace=server",
    "seed": "npm run seed --workspace=server"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
