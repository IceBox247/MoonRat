{
  "name": "moonrat",
  "version": "0.1.0",
  "private": true,
  "description": "Moonrat — a story-driven meme token & mining game Telegram Mini App on TON. MINE • HOLD • EXPLORE.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:api --workspace=@moonrat/miniapp & npm run dev --workspace=@moonrat/miniapp",
    "dev:api": "npm run dev:api --workspace=@moonrat/miniapp",
    "dev:miniapp": "npm run dev --workspace=@moonrat/miniapp",
    "dev:admin": "npm run dev --workspace=@moonrat/admin",
    "build": "npm run build --workspace=@moonrat/miniapp",
    "db:setup": "npm run db:setup --workspace=@moonrat/miniapp",
    "seed": "npm run seed --workspace=@moonrat/miniapp"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
